# `flip.utils`

Shared building blocks: logging, coordinate transforms, the small-scale velocity
damping, dictionary helpers and the prior classes.

::: flip.utils

## Subpackage probing

Manifest and availability probing that drives the `import flip` banner and the
lazy loading of subpackages.

::: flip._subpackages
