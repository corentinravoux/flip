# flip.comparison

A lightweight likelihood for comparing a model field directly against a gridded ground-truth field (e.g. from
a simulation), independent of the `flip.covariance` machinery.

::: flip.comparison.likelihood
